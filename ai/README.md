# ORCHORDS AI — AI Native Library

This module contains the ORCHORDS AI Android runtime and native MNN integration. Initialize submodules recursively and build through the repository Gradle wrapper. Do not commit native build output or local SDK paths.

**ORCHORDS — BUILD DIFFERENT.**
