# ORCHORDS AI — Stream Trace Fixtures

These deterministic synthetic fixtures test ORCHORDS AI streaming parsing and event ordering. Never include credentials, authorization headers, personal data, or production traffic.
