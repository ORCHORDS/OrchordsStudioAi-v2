package com.orchords.orchordsai.data.datastore

import com.orchords.ai.provider.ProviderSetting

/**
 * Providers suggested in the Add Provider flow.
 *
 * OrchordsAI does not promote third-party resellers or affiliate links; the
 * user always adds their own endpoints and keys.
 */
val RECOMMENDED_PROVIDERS: List<ProviderSetting> = emptyList()
