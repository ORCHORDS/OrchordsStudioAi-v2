# Permission UI

This package provides Compose helpers for Android runtime permissions. Request only permissions required by the active feature, explain their purpose, support denial, and test all permission states.
