<?xml version="1.0" encoding="utf-8"?>
<!--
  Play Store cleartext-traffic posture:

  Cleartext is denied for every host by default and only re-enabled for the IPv4/IPv6
  loopback ranges used by the local WebServerService and by debug-only emulator host
  loopback. This satisfies the Google Play policy that requires cleartext traffic to be
  justified per host instead of enabled application-wide.
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
      <certificates src="com.google.android.gms" />
    </trust-anchors>
  </base-config>

  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
    <domain includeSubdomains="false">::1</domain>
    <domain includeSubdomains="false">10.0.2.2</domain>
  </domain-config>
</network-security-config>
