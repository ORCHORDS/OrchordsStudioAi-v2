# GitHub MCP

Connect a trusted GitHub MCP server through ORCHORDS AI MCP settings. Use a fine-grained token limited to required repositories and permissions, prefer read-only access, and revoke unused credentials.
