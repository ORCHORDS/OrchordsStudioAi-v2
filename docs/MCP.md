# Model Context Protocol

ORCHORDS AI connects assistants to trusted MCP servers over supported HTTP streaming transports. Configure servers in MCP settings, authorize with least privilege, bind only required tools, and review approval prompts before mutations.
