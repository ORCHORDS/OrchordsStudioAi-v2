# Releasing ORCHORDS AI

Update version metadata, run checks, build with private signing configuration, verify signatures and installability, and publish release notes. Artifacts use `OrchordsAI-<version>-<abi>.apk`. Never commit signing keys or APKs.
