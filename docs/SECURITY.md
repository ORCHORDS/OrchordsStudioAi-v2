# Security Design

Credentials and conversations must not appear in logs, diagnostics, or repository files. Verify custom endpoints and MCP servers. Optional local servers should bind narrowly, validate requests, and stop when unused. See the repository security policy.
