# ORCHORDS AI — Web Interface

This React interface supports optional local web access for ORCHORDS AI. Run `pnpm install --frozen-lockfile` and `pnpm dev` for development. Keep user text in locale dictionaries, preserve the Android bridge contract, and exclude generated dependencies and local environment files.

**ORCHORDS — BUILD DIFFERENT.**
